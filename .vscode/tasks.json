{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake-build-debug",
            "type": "shell",
            "command": "${workspaceFolder}/build.sh Debug && xattr -cr ${workspaceFolder}/build/Debug/program && mkdir -p ${workspaceFolder}/runtest",
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}